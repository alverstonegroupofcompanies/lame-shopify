{% liquid
  assign cat_tile_width = section.settings.category_tile_width | default: 170
  assign cat_gap = 10
  assign heading_align = section.settings.heading_alignment | default: 'left'
  case section.settings.tile_shape
    when 'standard'
      assign tile_aspect = '4 / 3'
    when 'cinema'
      assign tile_aspect = '16 / 9'
    when 'square'
      assign tile_aspect = '1 / 1'
    when 'wide'
      assign tile_aspect = '3 / 2'
    else
      assign tile_aspect = '3 / 2'
  endcase
  assign show_subheading = section.settings.show_subheading | default: false
  assign show_tile_captions = section.settings.show_tile_captions | default: false
  assign show_title_on_image = section.settings.show_title_on_image | default: false
  assign show_carousel_arrows = section.settings.show_carousel_arrows | default: true
  assign show_head_area = false
  if section.settings.heading != blank
    assign show_head_area = true
  elsif show_subheading and section.settings.subheading != blank
    assign show_head_area = true
  endif
  if section.settings.auto_slide == true
    assign shop_cat_auto_slide = true
  elsif section.settings.auto_slide == false
    assign shop_cat_auto_slide = false
  else
    assign shop_cat_auto_slide = true
  endif
  if section.settings.pause_auto_slide_on_hover == false
    assign shop_cat_pause_hover = false
  else
    assign shop_cat_pause_hover = true
  endif
%}
<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<section class="section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }}">
  <div
    class="lame-shop-cat spacing-style"
    style="{% render 'spacing-padding', settings: section.settings %}"
    data-auto-slide="{% if shop_cat_auto_slide %}true{% else %}false{% endif %}"
    data-auto-slide-interval="{{ section.settings.auto_slide_interval | default: 5 }}"
    data-auto-slide-step="{{ section.settings.auto_advance_step | default: 'screen' }}"
    data-pause-auto-slide-on-hover="{% if shop_cat_pause_hover %}true{% else %}false{% endif %}"
  >
    {% if show_head_area %}
      <header class="lame-shop-cat__header lame-shop-cat__header--align-{{ heading_align }}" data-lame-reveal>
        {% if section.settings.heading != blank %}
          <h2 class="lame-shop-cat__title">{{ section.settings.heading }}</h2>
        {% endif %}
        {% if show_subheading and section.settings.subheading != blank %}
          <p class="lame-shop-cat__subtitle">{{ section.settings.subheading }}</p>
        {% endif %}
      </header>
    {% endif %}

    <div class="lame-shop-cat__carousel-wrap">
    <div
      class="lame-shop-cat__viewport"
      style="--lame-cat-tile-width: {{ cat_tile_width }}px; --lame-cat-gap: {{ cat_gap }}px; --lame-cat-aspect: {{ tile_aspect }};"
    >
    <ul class="lame-shop-cat__track" role="list" data-lame-shop-cat-track>
      {% for block in section.blocks %}
        {% liquid
          assign coll = block.settings.collection
          assign tile_caption = block.settings.caption
          if block.settings.title != blank
            assign tile_title = block.settings.title
          else
            assign tile_title = coll.title
          endif
          if block.settings.link != blank
            assign tile_url = block.settings.link
          else
            assign tile_url = coll.url
          endif
          if block.settings.image != blank
            assign tile_image = block.settings.image
          else
            assign tile_image = coll.featured_image
          endif
        %}
        {% if coll == blank %}
          {% if request.design_mode %}
            <li class="lame-shop-cat__item" {{ block.shopify_attributes }}>
              <div class="lame-shop-cat__tile lame-shop-cat__tile--placeholder">
                <div class="lame-shop-cat__media lame-shop-cat__media--placeholder">
                  <span class="lame-shop-cat__media-shade" aria-hidden="true"></span>
                  {% if show_title_on_image %}
                    <span class="lame-shop-cat__title-on-image">{{ section.settings.placeholder_collection_text }}</span>
                  {% elsif request.design_mode %}
                    <span class="visually-hidden">{{ section.settings.placeholder_collection_text }}</span>
                  {% endif %}
                </div>
              </div>
            </li>
          {% endif %}
          {% continue %}
        {% endif %}

        <li
          class="lame-shop-cat__item"
          data-lame-reveal
          data-lame-delay="{{ forloop.index0 | modulo: 4 }}"
          {{ block.shopify_attributes }}
        >
          <a class="lame-shop-cat__tile" href="{{ tile_url }}"{% unless show_title_on_image %} aria-label="{{ tile_title | escape }}"{% endunless %}>
            <div class="lame-shop-cat__media">
              {% if tile_image %}
                {{
                  tile_image
                  | image_url: width: 800
                  | image_tag:
                    class: 'lame-shop-cat__image',
                    loading: 'lazy',
                    widths: '120,200,360,480,640,800',
                    sizes: '(max-width: 749px) min(42vw, {{ cat_tile_width | times: 2 }}px), {{ cat_tile_width }}px'
                }}
              {% else %}
                {{ 'detailed-apparel-1' | placeholder_svg_tag: 'lame-shop-cat__image lame-shop-cat__placeholder-svg placeholder-svg' }}
              {% endif %}
              <span class="lame-shop-cat__media-shade" aria-hidden="true"></span>
              {% if show_title_on_image %}
                <span class="lame-shop-cat__title-on-image">{{ tile_title }}</span>
              {% endif %}
            </div>
          </a>
          {% if show_tile_captions and tile_caption != blank %}
            <p class="lame-shop-cat__caption">{{ tile_caption }}</p>
          {% endif %}
        </li>
      {% endfor %}
    </ul>
    </div>
      <div
        class="lame-shop-cat__controls{% unless show_carousel_arrows %} lame-shop-cat__controls--progress-only{% endunless %}"
        data-shop-cat-controls
        hidden
      >
        {% if show_carousel_arrows %}
          <button
            class="lame-shop-cat__ctrl-arrow lame-shop-cat__ctrl-arrow--prev"
            type="button"
            aria-label="{{ section.settings.prev_category_label }}"
            data-arrow="prev"
          >
            {% if section.settings.prev_arrow_visible_text != blank %}
              <span class="lame-shop-cat__ctrl-arrow-text">{{ section.settings.prev_arrow_visible_text }}</span>
            {% else %}
              <span aria-hidden="true">&#8249;</span>
            {% endif %}
          </button>
        {% endif %}
        <div class="lame-shop-cat__progress-track" aria-hidden="true">
          <div class="lame-shop-cat__progress-fill" data-shop-cat-progress-fill></div>
        </div>
        {% if show_carousel_arrows %}
          <button
            class="lame-shop-cat__ctrl-arrow lame-shop-cat__ctrl-arrow--next"
            type="button"
            aria-label="{{ section.settings.next_category_label }}"
            data-arrow="next"
          >
            {% if section.settings.next_arrow_visible_text != blank %}
              <span class="lame-shop-cat__ctrl-arrow-text">{{ section.settings.next_arrow_visible_text }}</span>
            {% else %}
              <span aria-hidden="true">&#8250;</span>
            {% endif %}
          </button>
        {% endif %}
      </div>
    </div>
  </div>
</section>

<script>
  (() => {
    const section = document.getElementById('shopify-section-{{ section.id }}');
    if (!section) return;
    const revealNodes = section.querySelectorAll('[data-lame-reveal]');
    if (!revealNodes.length) return;

    if ('IntersectionObserver' in window) {
      const observer = new IntersectionObserver(
        (entries, obs) => {
          entries.forEach((entry) => {
            if (!entry.isIntersecting) return;
            entry.target.classList.add('is-visible');
            obs.unobserve(entry.target);
          });
        },
        { threshold: 0.18, rootMargin: '0px 0px -8% 0px' }
      );
      revealNodes.forEach((node) => observer.observe(node));
    } else {
      revealNodes.forEach((node) => node.classList.add('is-visible'));
    }
  })();
</script>

<script>
  (() => {
    const sectionEl = document.getElementById('shopify-section-{{ section.id }}');
    if (!sectionEl) return;

    const root = sectionEl.querySelector('.lame-shop-cat');
    const track = sectionEl.querySelector('[data-lame-shop-cat-track]');
    const controls = sectionEl.querySelector('[data-shop-cat-controls]');
    const progressFill = sectionEl.querySelector('[data-shop-cat-progress-fill]');
    const prevBtn = sectionEl.querySelector('[data-arrow="prev"]');
    const nextBtn = sectionEl.querySelector('[data-arrow="next"]');

    if (!root || !track) return;

    const EPS = 4;

    const updateCarouselChrome = () => {
      const maxScroll = Math.max(0, track.scrollWidth - track.clientWidth);
      const hasOverflow = maxScroll > EPS;

      if (controls) {
        controls.hidden = !hasOverflow;
      }

      if (!hasOverflow) {
        if (progressFill) progressFill.style.width = '0%';
        if (prevBtn) prevBtn.disabled = true;
        if (nextBtn) nextBtn.disabled = true;
        return;
      }

      const pct = maxScroll > 0 ? (track.scrollLeft / maxScroll) * 100 : 0;
      if (progressFill) {
        progressFill.style.width = `${Math.min(100, Math.max(0, pct))}%`;
      }
      if (prevBtn) prevBtn.disabled = track.scrollLeft <= EPS;
      if (nextBtn) nextBtn.disabled = track.scrollLeft >= maxScroll - EPS;
    };

    const scrollByCard = (direction) => {
      const card = track.querySelector('.lame-shop-cat__item');
      if (!card) return;
      const styles = window.getComputedStyle(track);
      const gap = parseFloat(styles.columnGap || styles.gap || '0') || 0;
      track.scrollBy({ left: direction * (card.offsetWidth + gap), behavior: 'smooth' });
    };

    if (prevBtn) prevBtn.addEventListener('click', () => scrollByCard(-1));
    if (nextBtn) nextBtn.addEventListener('click', () => scrollByCard(1));
    track.addEventListener('scroll', updateCarouselChrome, { passive: true });

    const autoSlide = root.dataset.autoSlide === 'true';
    const intervalSec = Math.max(3, parseInt(root.dataset.autoSlideInterval || '5', 10));
    const pauseOnHover = root.dataset.pauseAutoSlideOnHover !== 'false';
    const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    const autoSlideStep = root.dataset.autoSlideStep || 'screen';

    let timerId = null;
    let isPaused = false;
    let isInView = false;

    const getStepPx = () => {
      const first = track.querySelector('.lame-shop-cat__item');
      if (!first) return 0;
      const styles = window.getComputedStyle(track);
      const gap = parseFloat(styles.columnGap || styles.gap || '0') || 0;
      return first.offsetWidth + gap;
    };

    const step = () => {
      if (isPaused || !isInView) return;
      const maxScroll = track.scrollWidth - track.clientWidth;
      if (maxScroll <= EPS) return;

      let amount = 0;
      if (autoSlideStep === 'screen') {
        const remaining = Math.max(0, maxScroll - track.scrollLeft);
        amount = Math.min(track.clientWidth, remaining);
        if (amount < EPS) {
          amount = getStepPx();
        }
      } else {
        amount = getStepPx();
      }
      if (amount <= 0) return;

      if (track.scrollLeft + track.clientWidth >= maxScroll - 3) {
        track.scrollTo({ left: 0, behavior: 'auto' });
      } else {
        track.scrollBy({ left: amount, behavior: 'smooth' });
      }
      updateCarouselChrome();
    };

    const start = () => {
      stop();
      timerId = window.setInterval(step, intervalSec * 1000);
    };

    const stop = () => {
      if (timerId != null) {
        window.clearInterval(timerId);
        timerId = null;
      }
    };

    if (autoSlide && !reducedMotion) {
      if ('IntersectionObserver' in window) {
        const io = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              isInView = entry.isIntersecting;
              if (!isInView) {
                stop();
              } else if (!timerId) {
                start();
              }
            });
          },
          { threshold: 0.12, rootMargin: '0px' }
        );
        io.observe(sectionEl);
      }

      if (pauseOnHover) {
        track.addEventListener('mouseenter', () => {
          isPaused = true;
        });
        track.addEventListener('mouseleave', () => {
          isPaused = false;
        });
        track.addEventListener('focusin', () => {
          isPaused = true;
        });
        track.addEventListener('focusout', () => {
          isPaused = false;
        });
      }

      if (!('IntersectionObserver' in window)) {
        isInView = true;
        start();
      }
    }

    window.addEventListener('resize', () => {
      updateCarouselChrome();
      if (autoSlide && !reducedMotion) {
        if (track.scrollWidth - track.clientWidth <= EPS) {
          stop();
        } else if (!timerId && isInView) {
          start();
        }
      }
    });

    setTimeout(updateCarouselChrome, 0);
  })();
</script>

{% stylesheet %}
  .lame-shop-cat__header {
    margin-bottom: var(--padding-md);
  }

  .lame-shop-cat__header--align-left {
    text-align: left;
  }

  .lame-shop-cat__header--align-left .lame-shop-cat__subtitle {
    margin-inline: 0;
    max-width: none;
  }

  .lame-shop-cat__header--align-center {
    text-align: center;
  }

  .lame-shop-cat__header--align-center .lame-shop-cat__subtitle {
    max-width: 42ch;
    margin-inline: auto;
  }

  .lame-shop-cat__title {
    margin: 0;
    color: rgb(var(--brand-gold));
    font-family: var(--lame-font-display, var(--font-heading--family));
    font-size: clamp(1rem, 2vw, 1.35rem);
    letter-spacing: 0.07em;
    text-transform: uppercase;
    line-height: 1.15;
  }

  .lame-shop-cat__subtitle {
    margin: 0.35rem 0 0;
    font-size: 0.78rem;
    color: rgb(var(--color-foreground-rgb) / 0.65);
  }

  .lame-shop-cat__carousel-wrap {
    position: relative;
    max-width: 1080px;
    margin-inline: auto;
  }

  .lame-shop-cat__viewport {
    margin-inline: auto;
  }

  .lame-shop-cat__track {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: nowrap;
    gap: var(--lame-cat-gap, 10px);
    overflow-x: auto;
    overflow-y: hidden;
    scroll-snap-type: x mandatory;
    scroll-behavior: smooth;
    scrollbar-width: none;
    -webkit-overflow-scrolling: touch;
  }

  .lame-shop-cat__track::-webkit-scrollbar {
    display: none;
  }

  .lame-shop-cat__item {
    flex: 0 0 var(--lame-cat-tile-width, 170px);
    width: var(--lame-cat-tile-width, 170px);
    max-width: min(var(--lame-cat-tile-width, 170px), calc(100vw - 2.5rem));
    min-width: 0;
    scroll-snap-align: start;
    display: flex;
    flex-direction: column;
    align-items: stretch;
  }

  .lame-shop-cat__tile {
    display: block;
    height: 100%;
    text-decoration: none;
    color: inherit;
    border: 1px solid rgb(var(--brand-gold) / 0.32);
    border-radius: var(--lame-radius-card, 10px);
    overflow: hidden;
    background: rgb(var(--color-background-rgb));
    box-shadow: var(--lame-shadow-soft);
    transition: transform 200ms ease, border-color 200ms ease, box-shadow 200ms ease;
  }

  .lame-shop-cat__tile:is(:hover, :focus-visible) {
    transform: translateY(-2px);
    border-color: rgb(var(--brand-gold) / 0.55);
    box-shadow: 0 10px 22px rgb(18 18 18 / 0.08);
  }

  .lame-shop-cat__media {
    position: relative;
    aspect-ratio: var(--lame-cat-aspect, 3 / 2);
    background: rgb(var(--brand-gold) / 0.06);
    overflow: hidden;
    isolation: isolate;
  }

  .lame-shop-cat__media--placeholder {
    background: rgb(var(--brand-gold) / 0.08);
  }

  .lame-shop-cat__image {
    position: relative;
    z-index: 0;
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .lame-shop-cat__placeholder-svg {
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.35;
  }

  .lame-shop-cat__media-shade {
    position: absolute;
    inset: 0;
    z-index: 1;
    pointer-events: none;
    background: linear-gradient(
      165deg,
      rgb(0 0 0 / 0.42) 0%,
      rgb(0 0 0 / 0.58) 100%
    );
  }

  .lame-shop-cat__title-on-image {
    position: absolute;
    inset: 0;
    z-index: 2;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0.45rem;
    box-sizing: border-box;
    text-align: center;
    font-size: clamp(0.62rem, 2.2vw, 0.82rem);
    line-height: 1.2;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    font-weight: 700;
    color: #fff;
    text-shadow: 0 1px 4px rgb(0 0 0 / 0.55);
    pointer-events: none;
  }

  .lame-shop-cat__caption {
    margin: 0.4rem 0 0;
    padding: 0 0.15rem;
    text-align: center;
    font-size: 0.68rem;
    line-height: 1.3;
    color: rgb(var(--color-foreground-rgb) / 0.82);
  }

  .lame-shop-cat__controls {
    display: flex;
    flex-wrap: nowrap;
    align-items: center;
    gap: 0.75rem;
    margin-top: 0.85rem;
    padding-inline: 0.15rem;
  }

  .lame-shop-cat__controls--progress-only {
    justify-content: stretch;
  }

  .lame-shop-cat__controls--progress-only .lame-shop-cat__progress-track {
    flex: 1;
  }

  .lame-shop-cat__ctrl-arrow-text {
    font-size: 0.62rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    line-height: 1;
    white-space: nowrap;
  }

  .lame-shop-cat__ctrl-arrow:has(.lame-shop-cat__ctrl-arrow-text) {
    width: auto;
    min-width: 2rem;
    padding-inline: 0.55rem;
  }

  .lame-shop-cat__controls[hidden] {
    display: none !important;
  }

  .lame-shop-cat__progress-track {
    flex: 1;
    min-width: 0;
    height: 4px;
    border-radius: 999px;
    background: rgb(var(--color-foreground-rgb) / 0.12);
    overflow: hidden;
  }

  .lame-shop-cat__progress-fill {
    height: 100%;
    width: 0%;
    border-radius: inherit;
    background: rgb(var(--brand-gold));
    transition: width 140ms ease-out;
  }

  .lame-shop-cat__ctrl-arrow {
    flex-shrink: 0;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    padding: 0;
    border: 1px solid rgb(var(--brand-gold) / 0.35);
    border-radius: 999px;
    background: rgb(var(--color-background-rgb));
    color: rgb(var(--color-foreground-rgb));
    cursor: pointer;
    transition: border-color 180ms ease, background 180ms ease, opacity 180ms ease;
  }

  .lame-shop-cat__ctrl-arrow:hover:not(:disabled) {
    border-color: rgb(var(--brand-gold) / 0.55);
  }

  .lame-shop-cat__ctrl-arrow:disabled {
    opacity: 0.35;
    cursor: default;
  }

  .lame-shop-cat__tile--placeholder {
    border-style: dashed;
  }

{% endstylesheet %}

{% schema %}
{
  "name": "Lame Shop by category",
  "settings": [
    {
      "type": "select",
      "id": "section_width",
      "label": "Section width",
      "options": [
        { "value": "page-width", "label": "Page width" },
        { "value": "full-width", "label": "Full width" }
      ],
      "default": "page-width"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Shop by category"
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading",
      "default": "Choose a collection to browse products."
    },
    {
      "type": "checkbox",
      "id": "show_subheading",
      "label": "Show subheading",
      "default": false
    },
    {
      "type": "select",
      "id": "heading_alignment",
      "label": "Heading alignment",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" }
      ],
      "default": "left"
    },
    {
      "type": "text",
      "id": "prev_category_label",
      "label": "Previous categories (screen readers)",
      "default": "Previous categories"
    },
    {
      "type": "text",
      "id": "next_category_label",
      "label": "Next categories (screen readers)",
      "default": "Next categories"
    },
    {
      "type": "text",
      "id": "placeholder_collection_text",
      "label": "Placeholder text when no collection selected",
      "default": "Select a collection"
    },
    {
      "type": "header",
      "content": "Category carousel"
    },
    {
      "type": "select",
      "id": "tile_shape",
      "label": "Tile shape",
      "options": [
        { "value": "wide", "label": "Landscape rectangle (3:2)" },
        { "value": "standard", "label": "Standard (4:3)" },
        { "value": "cinema", "label": "Wide (16:9)" },
        { "value": "square", "label": "Square (1:1)" }
      ],
      "default": "wide"
    },
    {
      "type": "checkbox",
      "id": "show_title_on_image",
      "label": "Show category title on image",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_tile_captions",
      "label": "Show captions under tiles",
      "default": false,
      "info": "Requires caption text on each category block."
    },
    {
      "type": "range",
      "id": "category_tile_width",
      "label": "Category tile width",
      "min": 110,
      "max": 200,
      "step": 5,
      "unit": "px",
      "default": 170
    },
    {
      "type": "checkbox",
      "id": "auto_slide",
      "label": "Auto-advance carousel",
      "default": true
    },
    {
      "type": "range",
      "id": "auto_slide_interval",
      "label": "Auto-advance interval",
      "min": 3,
      "max": 10,
      "step": 1,
      "unit": "s",
      "default": 5
    },
    {
      "type": "select",
      "id": "auto_advance_step",
      "label": "Auto-advance distance",
      "options": [
        { "value": "card", "label": "One card" },
        { "value": "screen", "label": "One screen width" }
      ],
      "default": "screen",
      "info": "Screen width advances more categories per interval so the full row is easier to browse."
    },
    {
      "type": "checkbox",
      "id": "pause_auto_slide_on_hover",
      "label": "Pause auto-advance on hover or keyboard focus",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_carousel_arrows",
      "label": "Show previous / next arrow buttons",
      "default": true
    },
    {
      "type": "text",
      "id": "prev_arrow_visible_text",
      "label": "Previous button visible text",
      "info": "Optional. Leave empty for the ‹ symbol only."
    },
    {
      "type": "text",
      "id": "next_arrow_visible_text",
      "label": "Next button visible text",
      "info": "Optional. Leave empty for the › symbol only."
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Top padding",
      "min": 0,
      "max": 96,
      "step": 4,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Bottom padding",
      "min": 0,
      "max": 96,
      "step": 4,
      "unit": "px",
      "default": 24
    }
  ],
  "blocks": [
    {
      "type": "category",
      "name": "Category",
      "settings": [
        {
          "type": "collection",
          "id": "collection",
          "label": "Collection"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title override"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image override"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link override",
          "info": "Leave empty to use the selected collection page (recommended). Only set this if you need a custom destination URL."
        },
        {
          "type": "text",
          "id": "caption",
          "label": "Caption",
          "info": "Optional short label shown below the tile (e.g. Skin, Hair)."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Lame Shop by category",
      "blocks": [
        { "type": "category" },
        { "type": "category" },
        { "type": "category" },
        { "type": "category" }
      ]
    }
  ]
}
{% endschema %}
