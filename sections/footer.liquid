{%- liquid
  assign categories_menu = linklists[section.settings.categories_menu]
  assign quick_links_menu = linklists[section.settings.quick_links_menu]
  assign use_collection_links = false
  if categories_menu == blank or categories_menu.links.size == 0
    assign use_collection_links = true
  endif

  assign phone_href = section.settings.phone | strip
  if phone_href != blank
    assign phone_href = phone_href | remove: ' ' | remove: '-' | remove: '(' | remove: ')'
  endif

  assign map_query = section.settings.map_query | default: 'Lamstone Healthcare Mangalapuram Kerala' | url_encode
  assign directions_url = section.settings.map_directions_url
  if directions_url == blank
    assign directions_url = 'https://www.google.com/maps/search/?api=1&query=' | append: map_query
  endif
-%}

<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<footer
  class="section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }} footer-premium footer-premium--forest"
  id="footer-{{ section.id }}"
>
  {% if section.settings.show_footer_image %}
    <div class="footer-premium__visual" aria-hidden="true">
      {% if section.settings.footer_image != blank %}
        {{
          section.settings.footer_image
          | image_url: width: 2400
          | image_tag:
            class: 'footer-premium__visual-img',
            loading: 'lazy',
            widths: '600,900,1200,1600,2000,2400',
            sizes: '100vw',
            alt: ''
        }}
      {% else %}
        {{ 'lifestyle-2' | placeholder_svg_tag: 'footer-premium__visual-placeholder' }}
      {% endif %}
      <span
        class="footer-premium__visual-overlay"
        style="--footer-visual-overlay: {{ section.settings.footer_image_overlay | divided_by: 100.0 }};"
      ></span>
    </div>
  {% endif %}

  <div class="footer-premium__inner spacing-style" style="{% render 'spacing-style', settings: section.settings %}">
    <div class="footer-premium__main">
      <div class="footer-premium__brand">
        {% if section.settings.logo != blank %}
          <a href="{{ routes.root_url }}" class="footer-premium__logo-link">
            {{
              section.settings.logo
              | image_url: width: 360
              | image_tag:
                class: 'footer-premium__logo',
                loading: 'lazy',
                widths: '120,180,240,360',
                sizes: '200px',
                alt: section.settings.brand_name | default: shop.name
            }}
          </a>
        {% elsif section.settings.brand_name != blank %}
          <a href="{{ routes.root_url }}" class="footer-premium__brand-name">{{ section.settings.brand_name }}</a>
        {% endif %}

        {% if section.settings.about_text != blank %}
          <div class="footer-premium__about rte">{{ section.settings.about_text }}</div>
        {% endif %}

        {% if section.blocks.size > 0 %}
          <ul class="footer-premium__social" role="list">
            {% for block in section.blocks %}
              {% if block.type == 'social_link' and block.settings.url != blank %}
                <li {{ block.shopify_attributes }}>
                  <a
                    href="{{ block.settings.url }}"
                    class="footer-premium__social-link"
                    aria-label="{{ block.settings.label | escape }}"
                    {% if block.settings.open_in_new_tab %}
                      target="_blank" rel="noopener noreferrer"
                    {% endif %}
                  >
                    {% render 'footer-social-icon', platform: block.settings.platform %}
                    <span class="visually-hidden">{{ block.settings.label }}</span>
                  </a>
                </li>
              {% endif %}
            {% endfor %}
          </ul>
        {% endif %}
      </div>

      <div class="footer-premium__column">
        <h3 class="footer-premium__heading footer-premium__heading--icon">
          <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-icon', name: 'grid' %}</span>
          {{ section.settings.categories_heading }}
        </h3>
        <ul class="footer-premium__links" role="list">
          {% if use_collection_links %}
            {% for collection in collections limit: 12 %}
              {% unless collection.handle == 'frontpage' %}
                <li>
                  <a href="{{ collection.url }}" class="footer-premium__link-row">
                    <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-icon', name: 'grid' %}</span>
                    <span>{{ collection.title }}</span>
                  </a>
                </li>
              {% endunless %}
            {% endfor %}
            {% if collections.size == 0 %}
              <li>
                <a href="{{ routes.all_products_collection_url }}" class="footer-premium__link-row">
                  <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-icon', name: 'grid' %}</span>
                  <span>Shop all</span>
                </a>
              </li>
            {% endif %}
          {% else %}
            {% for link in categories_menu.links %}
              <li>
                <a href="{{ link.url }}" class="footer-premium__link-row">
                  <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-link-icon', link: link %}</span>
                  <span>{{ link.title }}</span>
                </a>
              </li>
            {% endfor %}
          {% endif %}
        </ul>
      </div>

      <div class="footer-premium__column">
        <h3 class="footer-premium__heading footer-premium__heading--icon">
          <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-icon', name: 'arrow' %}</span>
          {{ section.settings.quick_links_heading }}
        </h3>
        <ul class="footer-premium__links" role="list">
          {% if quick_links_menu != blank and quick_links_menu.links.size > 0 %}
            {% for link in quick_links_menu.links %}
              <li>
                <a href="{{ link.url }}" class="footer-premium__link-row">
                  <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-link-icon', link: link %}</span>
                  <span>{{ link.title }}</span>
                </a>
              </li>
            {% endfor %}
          {% else %}
            <li>
              <a href="{{ routes.root_url }}" class="footer-premium__link-row">
                <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-icon', name: 'home' %}</span>
                <span>Home</span>
              </a>
            </li>
            <li>
              <a href="{{ routes.all_products_collection_url }}" class="footer-premium__link-row">
                <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-icon', name: 'grid' %}</span>
                <span>All products</span>
              </a>
            </li>
            {% if pages.contact %}
              <li>
                <a href="{{ pages.contact.url }}" class="footer-premium__link-row">
                  <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-icon', name: 'email' %}</span>
                  <span>Contact</span>
                </a>
              </li>
            {% endif %}
            <li>
              <a href="{{ routes.search_url }}" class="footer-premium__link-row">
                <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-icon', name: 'search' %}</span>
                <span>Search</span>
              </a>
            </li>
            <li>
              <a href="{{ routes.account_url }}" class="footer-premium__link-row">
                <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-icon', name: 'account' %}</span>
                <span>My account</span>
              </a>
            </li>
          {% endif %}
        </ul>
      </div>

      {% if section.settings.show_newsletter %}
        <div class="footer-premium__newsletter">
          <h3 class="footer-premium__heading footer-premium__heading--icon">
            <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-icon', name: 'mail' %}</span>
            {{ section.settings.newsletter_heading }}
          </h3>
          {% if section.settings.newsletter_text != blank %}
            <p class="footer-premium__newsletter-text">{{ section.settings.newsletter_text }}</p>
          {% endif %}
          {%- form 'customer', class: 'footer-premium__newsletter-form' -%}
            <input type="hidden" name="contact[tags]" value="newsletter">
            <label class="visually-hidden" for="FooterNewsletterEmail-{{ section.id }}">
              {{ 'blocks.email_signup.label' | t }}
            </label>
            <div class="footer-premium__newsletter-row">
              <span class="footer-premium__icon footer-premium__icon--input" aria-hidden="true">
                {% render 'footer-icon', name: 'email' %}
              </span>
              <input
                id="FooterNewsletterEmail-{{ section.id }}"
                class="footer-premium__newsletter-input"
                type="email"
                name="contact[email]"
                value="{{ form.email }}"
                placeholder="{{ section.settings.newsletter_placeholder }}"
                autocomplete="email"
                autocorrect="off"
                autocapitalize="off"
                required
                {% if form.errors %}
                  aria-invalid="true"
                {% endif %}
              >
              <button type="submit" class="footer-premium__newsletter-btn">
                <span>{{ section.settings.newsletter_button }}</span>
                <span class="footer-premium__icon footer-premium__icon--btn" aria-hidden="true">
                  {% render 'footer-icon', name: 'send' %}
                </span>
              </button>
            </div>
            {%- if form.errors -%}
              <p class="footer-premium__newsletter-message footer-premium__newsletter-message--error">
                {{ form.errors.translated_fields.email | capitalize }}
                {{ form.errors.messages.email }}
              </p>
            {%- endif -%}
            {%- if form.posted_successfully? -%}
              <p class="footer-premium__newsletter-message footer-premium__newsletter-message--success">
                {% if section.settings.newsletter_success != blank %}
                  {{ section.settings.newsletter_success }}
                {% else %}
                  {{ 'blocks.email_signup.success' | t }}
                {% endif %}
              </p>
            {%- endif -%}
          {%- endform -%}
        </div>
      {% endif %}
    </div>

    <div class="footer-premium__mid">
      {% if section.settings.show_map %}
        <div class="footer-premium__map-wrap">
          <h3 class="footer-premium__heading footer-premium__heading--icon">
            <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-icon', name: 'map-pin' %}</span>
            {{ section.settings.map_heading }}
          </h3>
          <div class="footer-premium__map">
            {% if section.settings.map_embed_url != blank %}
              <iframe
                title="{{ section.settings.map_heading | escape }}"
                src="{{ section.settings.map_embed_url }}"
                loading="lazy"
                referrerpolicy="no-referrer-when-downgrade"
                allowfullscreen
              ></iframe>
            {% else %}
              <a
                class="footer-premium__map-fallback"
                href="{{ directions_url }}"
                target="_blank"
                rel="noopener noreferrer"
              >
                <span class="footer-premium__icon footer-premium__icon--map" aria-hidden="true">
                  {% render 'footer-icon', name: 'map-pin' %}
                </span>
                <span>{{ section.settings.map_fallback_text }}</span>
              </a>
            {% endif %}
          </div>
          <a class="footer-premium__directions" href="{{ directions_url }}" target="_blank" rel="noopener noreferrer">
            {{ section.settings.map_directions_label }}
            <span class="footer-premium__icon footer-premium__icon--inline" aria-hidden="true">
              {% render 'footer-icon', name: 'external' %}
            </span>
          </a>
        </div>
      {% endif %}

      <div class="footer-premium__contact-grid">
        {% if section.settings.show_head_office %}
          <div class="footer-premium__contact-card footer-premium__contact-card--head-office">
            <h4 class="footer-premium__contact-title">
              <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-icon', name: 'location' %}</span>
              {{ section.settings.head_office_heading }}
            </h4>
            {% if section.settings.head_office_address != blank %}
              <div class="rte footer-premium__head-office-address">{{ section.settings.head_office_address }}</div>
            {% endif %}
            <ul class="footer-premium__contact-list footer-premium__head-office-list" role="list">
              {% if section.settings.head_office_email != blank %}
                <li>
                  <span class="footer-premium__contact-label">Email</span>
                  <a href="mailto:{{ section.settings.head_office_email | strip }}">{{ section.settings.head_office_email }}</a>
                </li>
              {% endif %}
              {% if section.settings.head_office_website != blank %}
                {% liquid
                  assign head_office_site = section.settings.head_office_website | strip
                  assign head_office_site_href = head_office_site
                  unless head_office_site_href contains '://'
                    assign head_office_site_href = 'https://' | append: head_office_site_href
                  endunless
                %}
                <li>
                  <span class="footer-premium__contact-label">Website</span>
                  <a href="{{ head_office_site_href }}" target="_blank" rel="noopener noreferrer">
                    {{ section.settings.head_office_website }}
                  </a>
                </li>
              {% endif %}
              {% if section.settings.head_office_phones != blank %}
                <li class="footer-premium__contact-extra">
                  <span class="footer-premium__contact-label">Contact No</span>
                  <div class="rte footer-premium__head-office-phones">{{ section.settings.head_office_phones }}</div>
                </li>
              {% endif %}
            </ul>
          </div>
        {% endif %}
        <div class="footer-premium__contact-card">
          <h4 class="footer-premium__contact-title">
            <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-icon', name: 'clock' %}</span>
            {{ section.settings.hours_heading }}
          </h4>
          <div class="rte">{{ section.settings.working_hours }}</div>
        </div>
        <div class="footer-premium__contact-card">
          <h4 class="footer-premium__contact-title">
            <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-icon', name: 'location' %}</span>
            {{ section.settings.address_heading }}
          </h4>
          <div class="rte">{{ section.settings.shop_address }}</div>
        </div>
        <div class="footer-premium__contact-card">
          <h4 class="footer-premium__contact-title">
            <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-icon', name: 'phone' %}</span>
            {{ section.settings.contact_heading }}
          </h4>
          <ul class="footer-premium__contact-list" role="list">
            {% if section.settings.phone != blank %}
              <li>
                <span class="footer-premium__contact-label">Phone</span>
                <a href="tel:{{ phone_href }}">{{ section.settings.phone }}</a>
              </li>
            {% endif %}
            {% if section.settings.email != blank %}
              <li>
                <span class="footer-premium__contact-label">Email</span>
                <a href="mailto:{{ section.settings.email | strip }}">{{ section.settings.email }}</a>
              </li>
            {% endif %}
            {% if section.settings.contact_details != blank %}
              <li class="footer-premium__contact-extra rte">{{ section.settings.contact_details }}</li>
            {% endif %}
          </ul>
        </div>
      </div>
    </div>

    {% if section.settings.trust_badges != blank %}
      <p class="footer-premium__trust">
        <span class="footer-premium__icon footer-premium__icon--trust" aria-hidden="true">
          {% render 'footer-icon', name: 'sparkle' %}
        </span>
        {{ section.settings.trust_badges }}
      </p>
    {% endif %}

    <div class="footer-premium__bottom">
      <p class="footer-premium__copyright">{{ section.settings.copyright_text | newline_to_br }}</p>
      {% if section.settings.show_policy_links %}
        <ul class="footer-premium__policies" role="list">
          {% for policy in shop.policies %}
            {% if policy != blank %}
              <li>
                <a href="{{ policy.url }}" class="footer-premium__link-row footer-premium__link-row--policy">
                  <span class="footer-premium__icon" aria-hidden="true">{% render 'footer-icon', name: 'arrow' %}</span>
                  <span>{{ policy.title }}</span>
                </a>
              </li>
            {% endif %}
          {% endfor %}
        </ul>
      {% endif %}
    </div>
  </div>

  {% if section.settings.show_ventures %}
    {% liquid
      assign venture_blocks = section.blocks | where: 'type', 'venture'
    %}
    {% if venture_blocks.size > 0 or request.design_mode %}
      <div class="footer-premium__ventures-bar" aria-label="{{ section.settings.ventures_heading | default: 'Our Ventures' | escape }}">
        <div class="footer-premium__ventures-bar-inner">
          {% if section.settings.ventures_heading != blank %}
            <p class="footer-premium__ventures-bar-label">{{ section.settings.ventures_heading }}</p>
          {% endif %}
          <ul class="footer-premium__ventures-row" role="list">
            {% for block in venture_blocks %}
              {% liquid
                assign venture_image = block.settings.image
                assign venture_alt = block.settings.alt | default: block.settings.title
                assign venture_asset = block.settings.asset | strip
                assign has_venture_logo = false
                if venture_image != blank or venture_asset != blank
                  assign has_venture_logo = true
                endif
              %}
              {% if has_venture_logo == false and request.design_mode == false %}
                {% continue %}
              {% endif %}
              <li class="footer-premium__venture-item" {{ block.shopify_attributes }}>
                {% if block.settings.link != blank %}
                  <a
                    href="{{ block.settings.link }}"
                    class="footer-premium__venture-link"
                    aria-label="{{ venture_alt | escape }}"
                    {% if block.settings.open_in_new_tab %}
                      target="_blank" rel="noopener noreferrer"
                    {% endif %}
                  >
                {% else %}
                  <span class="footer-premium__venture-link">
                {% endif %}
                {% if venture_image != blank %}
                  {{
                    venture_image
                    | image_url: width: 960
                    | image_tag:
                      class: 'footer-premium__venture-logo',
                      loading: 'lazy',
                      widths: '240,320,400,480,640,800,960',
                      sizes: '(max-width: 749px) 30vw, 160px',
                      alt: venture_alt
                  }}
                {% elsif venture_asset != blank %}
                  <img
                    class="footer-premium__venture-logo"
                    src="{{ venture_asset | asset_url }}"
                    alt="{{ venture_alt | escape }}"
                    width="560"
                    height="180"
                    loading="lazy"
                  >
                {% elsif request.design_mode %}
                  <span class="footer-premium__venture-placeholder">Add venture logo</span>
                {% endif %}
                {% if block.settings.link != blank %}
                  </a>
                {% else %}
                  </span>
                {% endif %}
              </li>
            {% endfor %}
          </ul>
        </div>
      </div>
    {% endif %}
  {% endif %}
</footer>

{% style %}
  #footer-{{ section.id }} {
    --footer-font-display: 'Cormorant Garamond', Georgia, serif;
    --footer-font-body: 'Jost', var(--font-body--family), sans-serif;
    --footer-forest: 45 74 34;
    --footer-forest-dark: 35 58 28;
    --footer-gold: rgb(221 196 130);
    --footer-gold-bright: rgb(212 175 55);
    --footer-gold-soft: rgb(255 255 255 / 0.18);
    --footer-icon-bg: rgb(255 255 255 / 0.1);
    --footer-text: rgb(255 255 255);
    --footer-text-muted: rgb(255 255 255 / 0.9);
    --footer-text-subtle: rgb(255 255 255 / 0.72);
    --footer-icon-size: 0.875rem;
    --footer-icon-box: 1.375rem;
    font-family: var(--footer-font-body);
    color: var(--footer-text);
    border-top: 1px solid rgb(255 255 255 / 0.12);
    background: linear-gradient(180deg, rgb(47 79 58) 0%, rgb(var(--footer-forest-dark)) 100%);
  }

  #footer-{{ section.id }} .footer-premium__icon-svg,
  #footer-{{ section.id }} svg.footer-premium__icon-svg {
    width: var(--footer-icon-size);
    height: var(--footer-icon-size);
    display: block;
    flex-shrink: 0;
  }

  #footer-{{ section.id }} .footer-premium__inner {
    display: grid;
    gap: clamp(1.5rem, 3vw, 2.5rem);
  }

  #footer-{{ section.id }} .footer-premium__main {
    display: grid;
    gap: clamp(1.25rem, 2.5vw, 2rem);
  }

  #footer-{{ section.id }} .footer-premium__brand {
    display: grid;
    gap: 0.85rem;
    align-content: start;
  }

  #footer-{{ section.id }} .footer-premium__logo-link {
    display: inline-block;
    line-height: 0;
  }

  #footer-{{ section.id }} .footer-premium__logo {
    width: 100%;
    max-width: 200px;
    height: auto;
    display: block;
  }

  #footer-{{ section.id }} .footer-premium__brand-name {
    font-family: var(--footer-font-display);
    font-size: clamp(1.35rem, 2.4vw, 1.75rem);
    font-weight: 500;
    letter-spacing: 0.02em;
    text-decoration: none;
    color: inherit;
  }

  #footer-{{ section.id }} .footer-premium__about {
    margin: 0;
    max-width: 36ch;
    font-size: 0.9rem;
    line-height: 1.6;
    color: var(--footer-text-muted);
  }

  #footer-{{ section.id }} .footer-premium__about p {
    margin: 0;
  }

  #footer-{{ section.id }} .footer-premium__social {
    list-style: none;
    margin: 0.15rem 0 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  #footer-{{ section.id }} .footer-premium__social-link {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    border-radius: 999px;
    border: 1px solid var(--footer-gold-soft);
    background: var(--footer-icon-bg);
    color: var(--footer-gold);
    transition: background 200ms ease, border-color 200ms ease, color 200ms ease, transform 200ms ease;
  }

  #footer-{{ section.id }} .footer-premium__social-link:hover {
    background: var(--footer-gold-bright);
    border-color: var(--footer-gold-bright);
    color: rgb(var(--footer-forest-dark));
    transform: translateY(-1px);
  }

  #footer-{{ section.id }} .footer-premium__social-link svg {
    width: 0.95rem;
    height: 0.95rem;
    fill: currentColor;
    display: block;
  }

  #footer-{{ section.id }} .footer-premium__ventures-bar {
    margin-top: clamp(0.5rem, 1.5vw, 0.85rem);
    border-top: 1px solid rgb(0 0 0 / 0.1);
    border-bottom: 1px solid rgb(0 0 0 / 0.1);
    background-color: #f2f3f1;
    background-image:
      radial-gradient(rgb(0 0 0 / 0.045) 0.65px, transparent 0.65px),
      linear-gradient(180deg, rgb(255 255 255 / 0.55) 0%, transparent 42%, transparent 58%, rgb(0 0 0 / 0.02) 100%);
    background-size: 10px 10px, 100% 100%;
    color: rgb(24 24 24);
  }

  #footer-{{ section.id }} .footer-premium__ventures-bar-inner {
    width: 100%;
    max-width: var(--page-width, 1200px);
    margin-inline: auto;
    padding: clamp(1.35rem, 3vw, 2rem) clamp(1rem, 3vw, 2rem);
    box-sizing: border-box;
  }

  #footer-{{ section.id }} .footer-premium__ventures-bar-label {
    margin: 0 0 0.85rem;
    text-align: center;
    font-family: var(--footer-font-display);
    font-size: clamp(1.05rem, 1.5vw, 1.32rem);
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: none;
    color: #062f01;
    position: relative;
    padding-bottom: 0.55rem;
  }

  #footer-{{ section.id }} .footer-premium__ventures-bar-label::after {
    content: '';
    position: absolute;
    left: 50%;
    bottom: 0;
    width: 2.25rem;
    height: 2px;
    transform: translateX(-50%);
    background: rgb(6 47 1 / 0.35);
    border-radius: 1px;
  }

  #footer-{{ section.id }} .footer-premium__ventures-row {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
    gap: clamp(0.85rem, 2.8vw, 2.25rem);
  }

  #footer-{{ section.id }} .footer-premium__venture-item {
    flex: 0 1 auto;
    min-width: 0;
  }

  #footer-{{ section.id }} .footer-premium__venture-link {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 3.5rem;
    padding: 0.35rem 0.5rem;
    text-decoration: none;
    background: transparent;
    border: none;
    transition: opacity 180ms ease, transform 180ms ease;
  }

  #footer-{{ section.id }} .footer-premium__venture-link:is(:hover, :focus-visible) {
    opacity: 0.82;
    transform: translateY(-1px);
  }

  #footer-{{ section.id }} .footer-premium__venture-logo {
    display: block;
    width: auto;
    max-width: min(10rem, 14vw);
    height: auto;
    max-height: 3.25rem;
    object-fit: contain;
    object-position: center;
    background: transparent;
    filter: invert(1);
    mix-blend-mode: multiply;
  }

  #footer-{{ section.id }} .footer-premium__venture-placeholder {
    min-width: 5.5rem;
    padding: 0.5rem 0.65rem;
    border: 1px dashed rgb(0 0 0 / 0.18);
    border-radius: 6px;
    font-size: 0.62rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: rgb(0 0 0 / 0.45);
    text-align: center;
    background: rgb(255 255 255 / 0.65);
  }

  @media screen and (min-width: 750px) {
    #footer-{{ section.id }} .footer-premium__ventures-bar-inner {
      padding: clamp(1.5rem, 2.5vw, 2.35rem) clamp(1.5rem, 3vw, 2.5rem);
    }

    #footer-{{ section.id }} .footer-premium__ventures-bar-label {
      margin-bottom: 1.1rem;
      font-size: clamp(1.2rem, 1.35vw, 1.45rem);
      letter-spacing: 0.03em;
    }

    #footer-{{ section.id }} .footer-premium__ventures-row {
      flex-wrap: nowrap;
      justify-content: space-between;
      align-items: center;
      gap: clamp(0.75rem, 1.6vw, 1.5rem);
      max-width: min(1080px, 100%);
      margin-inline: auto;
    }

    #footer-{{ section.id }} .footer-premium__venture-item {
      flex: 1 1 0;
      min-width: 0;
      max-width: 11.5rem;
    }

    #footer-{{ section.id }} .footer-premium__venture-link {
      min-height: 4.75rem;
      padding: 0.4rem 0.35rem;
      width: 100%;
    }

    #footer-{{ section.id }} .footer-premium__venture-logo {
      width: 100%;
      max-width: 100%;
      max-height: clamp(3.5rem, 4.2vw, 4.25rem);
    }
  }

  @media screen and (min-width: 990px) {
    #footer-{{ section.id }} .footer-premium__ventures-row {
      gap: clamp(1rem, 2vw, 2rem);
    }

    #footer-{{ section.id }} .footer-premium__venture-logo {
      max-height: 4.5rem;
    }
  }

  @media screen and (max-width: 749px) {
    #footer-{{ section.id }} .footer-premium__ventures-bar-inner {
      padding-inline: clamp(0.65rem, 3vw, 1rem);
    }

    #footer-{{ section.id }} .footer-premium__ventures-row {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      align-items: center;
      gap: 0.45rem 0.55rem;
      max-width: 22rem;
      margin-inline: auto;
      overflow: visible;
      scroll-snap-type: none;
      padding-bottom: 0;
    }

    #footer-{{ section.id }} .footer-premium__ventures-row::-webkit-scrollbar {
      display: none;
    }

    #footer-{{ section.id }} .footer-premium__venture-item {
      flex: 0 0 calc(33.333% - 0.4rem);
      max-width: calc(33.333% - 0.4rem);
      scroll-snap-align: unset;
    }

    #footer-{{ section.id }} .footer-premium__venture-link {
      min-height: 2.35rem;
      padding: 0.2rem 0.15rem;
      width: 100%;
    }

    #footer-{{ section.id }} .footer-premium__venture-logo {
      width: 100%;
      max-width: 100%;
      max-height: 1.85rem;
    }
  }

  #footer-{{ section.id }} .footer-premium__heading {
    margin: 0 0 0.75rem;
    font-family: var(--footer-font-display);
    font-size: clamp(1.05rem, 1.8vw, 1.28rem);
    font-weight: 500;
    letter-spacing: 0.03em;
    text-transform: none;
    color: var(--footer-text);
    position: relative;
    padding-bottom: 0.45rem;
  }

  #footer-{{ section.id }} .footer-premium__heading--icon {
    display: flex;
    align-items: center;
    gap: 0.45rem;
  }

  #footer-{{ section.id }} .footer-premium__heading::after {
    content: '';
    position: absolute;
    left: 0;
    bottom: 0;
    width: 2rem;
    height: 1px;
    background: var(--footer-gold-bright);
  }

  #footer-{{ section.id }} .footer-premium__icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    width: var(--footer-icon-box);
    height: var(--footer-icon-box);
    border-radius: 6px;
    background: var(--footer-icon-bg);
    color: var(--footer-gold);
    line-height: 0;
  }

  #footer-{{ section.id }} .footer-premium__heading .footer-premium__icon {
    width: 1.5rem;
    height: 1.5rem;
    border-radius: 5px;
  }

  #footer-{{ section.id }} .footer-premium__heading .footer-premium__icon .footer-premium__icon-svg {
    width: 0.8125rem;
    height: 0.8125rem;
  }

  #footer-{{ section.id }} .footer-premium__links {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.35rem;
  }

  #footer-{{ section.id }} .footer-premium__link-row {
    display: inline-flex;
    align-items: center;
    gap: 0.45rem;
    width: fit-content;
    max-width: 100%;
    text-decoration: none;
    font-size: 0.875rem;
    line-height: 1.35;
    letter-spacing: 0.01em;
    color: var(--footer-text-muted);
    transition: color 180ms ease;
  }

  #footer-{{ section.id }} .footer-premium__link-row span:last-child {
    min-width: 0;
  }

  #footer-{{ section.id }} .footer-premium__link-row:is(:hover, :focus-visible) {
    color: var(--footer-gold);
  }

  #footer-{{ section.id }} .footer-premium__link-row .footer-premium__icon {
    width: 1.25rem;
    height: 1.25rem;
    border-radius: 4px;
  }

  #footer-{{ section.id }} .footer-premium__link-row .footer-premium__icon .footer-premium__icon-svg {
    width: 0.75rem;
    height: 0.75rem;
  }

  #footer-{{ section.id }} .footer-premium__newsletter-text {
    margin: 0 0 0.85rem;
    font-size: 0.875rem;
    line-height: 1.5;
    color: var(--footer-text-muted);
    max-width: 32ch;
  }

  #footer-{{ section.id }} .footer-premium__newsletter-form {
    display: grid;
    gap: 0.5rem;
  }

  #footer-{{ section.id }} .footer-premium__newsletter-row {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    align-items: center;
    gap: 0.45rem;
    border: 1px solid var(--footer-gold-soft);
    border-radius: 999px;
    padding: 0.3rem 0.35rem 0.3rem 0.6rem;
    background: rgb(255 255 255 / 0.08);
  }

  #footer-{{ section.id }} .footer-premium__icon--input {
    width: 1.25rem;
    height: 1.25rem;
    background: transparent;
    color: var(--footer-gold);
  }

  #footer-{{ section.id }} .footer-premium__icon--input .footer-premium__icon-svg {
    width: 0.8125rem;
    height: 0.8125rem;
  }

  #footer-{{ section.id }} .footer-premium__newsletter-input {
    width: 100%;
    min-width: 0;
    border: none;
    background: transparent;
    padding: 0.6rem 0.5rem;
    font-family: var(--footer-font-body);
    font-size: 0.86rem;
    color: inherit;
  }

  #footer-{{ section.id }} .footer-premium__newsletter-input:focus-visible {
    outline: 2px solid var(--footer-gold);
    outline-offset: 2px;
    border-radius: 999px;
  }

  #footer-{{ section.id }} .footer-premium__newsletter-btn {
    justify-self: end;
    min-height: 2rem;
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    border: none;
    border-radius: 999px;
    padding: 0.5rem 1rem;
    font-family: var(--footer-font-body);
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    cursor: pointer;
    background: linear-gradient(135deg, var(--footer-gold-bright), rgb(193 151 31));
    color: rgb(var(--footer-forest-dark));
    transition: opacity 180ms ease, transform 180ms ease;
    white-space: nowrap;
  }

  #footer-{{ section.id }} .footer-premium__icon--btn {
    width: auto;
    height: auto;
    padding: 0;
    background: transparent;
    color: inherit;
  }

  #footer-{{ section.id }} .footer-premium__icon--btn .footer-premium__icon-svg {
    width: 0.6875rem;
    height: 0.6875rem;
  }

  #footer-{{ section.id }} .footer-premium__newsletter-btn:hover {
    opacity: 0.92;
    transform: translateY(-1px);
  }

  @media screen and (max-width: 749px) {
    #footer-{{ section.id }} .footer-premium__newsletter-row {
      grid-template-columns: 1fr;
      border-radius: 16px;
      padding: 0.45rem;
      gap: 0.4rem;
    }

    #footer-{{ section.id }} .footer-premium__icon--input {
      display: none;
    }

    #footer-{{ section.id }} .footer-premium__newsletter-input {
      padding: 0.7rem 0.85rem;
      border-radius: 12px;
      background: rgb(255 255 255 / 0.05);
    }

    #footer-{{ section.id }} .footer-premium__newsletter-btn {
      width: 100%;
      justify-content: center;
      min-height: 2.2rem;
    }
  }

  #footer-{{ section.id }} .footer-premium__newsletter-message {
    margin: 0;
    font-size: 0.82rem;
  }

  #footer-{{ section.id }} .footer-premium__newsletter-message--error {
    color: #fecaca;
  }

  #footer-{{ section.id }} .footer-premium__newsletter-message--success {
    color: var(--footer-text-muted);
  }

  #footer-{{ section.id }} .footer-premium__mid {
    display: grid;
    gap: 1.25rem;
    padding-top: 0.25rem;
    border-top: 1px solid var(--footer-gold-soft);
  }

  #footer-{{ section.id }} .footer-premium__map-wrap {
    display: grid;
    gap: 0.65rem;
  }

  #footer-{{ section.id }} .footer-premium__map {
    position: relative;
    width: 100%;
    min-height: 220px;
    border-radius: 12px;
    overflow: hidden;
    border: 1px solid var(--footer-gold-soft);
    background: rgb(0 0 0 / 0.2);
  }

  #footer-{{ section.id }} .footer-premium__map iframe {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    border: 0;
  }

  #footer-{{ section.id }} .footer-premium__map-fallback {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    min-height: 220px;
    padding: 1.5rem;
    text-align: center;
    text-decoration: none;
    color: var(--footer-text-muted);
    font-size: 0.875rem;
    line-height: 1.45;
  }

  #footer-{{ section.id }} .footer-premium__icon--map {
    width: 2.25rem;
    height: 2.25rem;
    border-radius: 50%;
    background: var(--footer-icon-bg);
    color: var(--footer-gold);
  }

  #footer-{{ section.id }} .footer-premium__icon--map .footer-premium__icon-svg {
    width: 1rem;
    height: 1rem;
  }

  #footer-{{ section.id }} .footer-premium__directions {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    font-size: 0.78rem;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    text-decoration: none;
    color: var(--footer-gold);
    font-weight: 600;
  }

  #footer-{{ section.id }} .footer-premium__icon--inline {
    width: auto;
    height: auto;
    padding: 0;
    background: transparent;
  }

  #footer-{{ section.id }} .footer-premium__icon--inline .footer-premium__icon-svg {
    width: 0.75rem;
    height: 0.75rem;
  }

  #footer-{{ section.id }} .footer-premium__directions:hover {
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  #footer-{{ section.id }} .footer-premium__contact-grid {
    display: grid;
    gap: 1rem;
  }

  #footer-{{ section.id }} .footer-premium__contact-title,
  #footer-{{ section.id }} .footer-premium__contact-card h4 {
    margin: 0 0 0.5rem;
    display: flex;
    align-items: center;
    gap: 0.4rem;
    font-family: var(--footer-font-display);
    font-size: 0.98rem;
    font-weight: 500;
    letter-spacing: 0.02em;
    color: var(--footer-text);
  }

  #footer-{{ section.id }} .footer-premium__contact-title .footer-premium__icon,
  #footer-{{ section.id }} .footer-premium__contact-card h4 .footer-premium__icon {
    width: 1.35rem;
    height: 1.35rem;
  }

  #footer-{{ section.id }} .footer-premium__contact-title .footer-premium__icon .footer-premium__icon-svg,
  #footer-{{ section.id }} .footer-premium__contact-card h4 .footer-premium__icon .footer-premium__icon-svg {
    width: 0.8125rem;
    height: 0.8125rem;
  }

  #footer-{{ section.id }} .footer-premium__contact-card :is(p, a) {
    margin: 0;
    font-size: 0.875rem;
    line-height: 1.55;
    color: var(--footer-text-muted);
  }

  #footer-{{ section.id }} .footer-premium__contact-list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: grid;
    gap: 0.55rem;
  }

  #footer-{{ section.id }} .footer-premium__contact-label {
    display: block;
    font-size: 0.6875rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--footer-text-subtle);
    margin-bottom: 0.15rem;
  }

  #footer-{{ section.id }} .footer-premium__contact-list a {
    text-decoration: none;
    color: inherit;
  }

  #footer-{{ section.id }} .footer-premium__contact-list a:hover {
    color: var(--footer-gold);
  }

  #footer-{{ section.id }} .footer-premium__trust {
    margin: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.45rem;
    flex-wrap: wrap;
    text-align: center;
    font-size: 0.75rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--footer-text-subtle);
    padding: 0.55rem 0;
    border-top: 1px solid var(--footer-gold-soft);
    border-bottom: 1px solid var(--footer-gold-soft);
  }

  #footer-{{ section.id }} .footer-premium__icon--trust {
    width: 1.25rem;
    height: 1.25rem;
    background: transparent;
  }

  #footer-{{ section.id }} .footer-premium__icon--trust .footer-premium__icon-svg {
    width: 0.75rem;
    height: 0.75rem;
    color: var(--footer-gold);
  }

  #footer-{{ section.id }} .footer-premium__bottom {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.65rem;
    text-align: center;
  }

  #footer-{{ section.id }} .footer-premium__copyright {
    margin: 0;
    font-size: 0.8125rem;
    line-height: 1.5;
    color: var(--footer-text-subtle);
    max-width: 52ch;
  }

  #footer-{{ section.id }} .footer-premium__policies {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.35rem 1rem;
  }

  #footer-{{ section.id }} .footer-premium__policies a,
  #footer-{{ section.id }} .footer-premium__policies .footer-premium__link-row {
    font-size: 0.8125rem;
    text-decoration: none;
    color: var(--footer-text-subtle);
  }

  #footer-{{ section.id }} .footer-premium__policies a:hover {
    color: var(--footer-gold);
  }

  @media screen and (min-width: 750px) {
    #footer-{{ section.id }} .footer-premium__main {
      grid-template-columns: 1.15fr repeat(3, minmax(0, 1fr));
      align-items: start;
    }

    #footer-{{ section.id }} .footer-premium__mid {
      grid-template-columns: 1.1fr 1fr;
      gap: 1.75rem;
      align-items: start;
    }

    #footer-{{ section.id }} .footer-premium__map {
      min-height: 260px;
    }

    #footer-{{ section.id }} .footer-premium__contact-grid {
      grid-template-columns: 1fr;
    }

    #footer-{{ section.id }} .footer-premium__bottom {
      flex-direction: row;
      justify-content: space-between;
      text-align: start;
    }

    #footer-{{ section.id }} .footer-premium__policies {
      justify-content: flex-end;
    }
  }
{% endstyle %}

{% schema %}
{
  "name": "Lame Editable Footer",
  "class": "section-wrapper",
  "enabled_on": {
    "groups": ["footer"]
  },
  "settings": [
    {
      "type": "select",
      "id": "section_width",
      "label": "Section width",
      "options": [
        { "value": "page-width", "label": "Page width" },
        { "value": "full-width", "label": "Full width" }
      ],
      "default": "page-width"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-golden"
    },
    {
      "type": "header",
      "content": "Brand"
    },
    {
      "type": "text",
      "id": "brand_name",
      "label": "Brand name (fallback)",
      "default": "Lame Beauty Care"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Company logo"
    },
    {
      "type": "richtext",
      "id": "about_text",
      "label": "About text",
      "default": "<p>Premium cosmetic essentials crafted for radiant skin, confident beauty, and everyday self-care rituals.</p>"
    },
    {
      "type": "header",
      "content": "Our ventures"
    },
    {
      "type": "checkbox",
      "id": "show_ventures",
      "label": "Show venture logos",
      "default": true
    },
    {
      "type": "text",
      "id": "ventures_heading",
      "label": "Ventures heading",
      "default": "Our Ventures"
    },
    {
      "type": "header",
      "content": "Navigation"
    },
    {
      "type": "text",
      "id": "categories_heading",
      "label": "Categories heading",
      "default": "Shop by category"
    },
    {
      "type": "link_list",
      "id": "categories_menu",
      "label": "Categories menu",
      "info": "Leave empty to list all collections automatically."
    },
    {
      "type": "text",
      "id": "quick_links_heading",
      "label": "Quick links heading",
      "default": "Explore"
    },
    {
      "type": "link_list",
      "id": "quick_links_menu",
      "label": "Quick links menu",
      "info": "Leave empty for default store links and collections."
    },
    {
      "type": "header",
      "content": "Newsletter"
    },
    {
      "type": "checkbox",
      "id": "show_newsletter",
      "label": "Show newsletter signup",
      "default": true
    },
    {
      "type": "text",
      "id": "newsletter_heading",
      "label": "Newsletter heading",
      "default": "Join our beauty circle"
    },
    {
      "type": "text",
      "id": "newsletter_text",
      "label": "Newsletter description",
      "default": "Exclusive offers, new launches, and skincare tips — delivered to your inbox."
    },
    {
      "type": "text",
      "id": "newsletter_placeholder",
      "label": "Email placeholder",
      "default": "Your email address"
    },
    {
      "type": "text",
      "id": "newsletter_button",
      "label": "Subscribe button",
      "default": "Subscribe"
    },
    {
      "type": "text",
      "id": "newsletter_success",
      "label": "Success message",
      "default": "Welcome! You're subscribed to our newsletter."
    },
    {
      "type": "header",
      "content": "Map"
    },
    {
      "type": "checkbox",
      "id": "show_map",
      "label": "Show map",
      "default": true
    },
    {
      "type": "text",
      "id": "map_heading",
      "label": "Map heading",
      "default": "Visit our store"
    },
    {
      "type": "text",
      "id": "map_query",
      "label": "Map search query",
      "default": "Lamstone Healthcare Mangalapuram Kerala 695317",
      "info": "Used for directions link when embed URL is empty."
    },
    {
      "type": "url",
      "id": "map_embed_url",
      "label": "Google Maps embed URL",
      "info": "Paste the iframe src URL from Google Maps → Share → Embed a map."
    },
    {
      "type": "url",
      "id": "map_directions_url",
      "label": "Directions link (optional)"
    },
    {
      "type": "text",
      "id": "map_directions_label",
      "label": "Directions link label",
      "default": "Get directions"
    },
    {
      "type": "text",
      "id": "map_fallback_text",
      "label": "Map fallback text",
      "default": "Tap to open location in Google Maps"
    },
    {
      "type": "header",
      "content": "Contact & hours"
    },
    {
      "type": "text",
      "id": "hours_heading",
      "label": "Working hours heading",
      "default": "Boutique hours"
    },
    {
      "type": "richtext",
      "id": "working_hours",
      "label": "Working hours",
      "default": "<p>Mon – Sat: 10:00 AM – 7:00 PM</p><p>Sunday: Closed</p>"
    },
    {
      "type": "text",
      "id": "address_heading",
      "label": "Address heading",
      "default": "Our location"
    },
    {
      "type": "richtext",
      "id": "shop_address",
      "label": "Shop address",
      "default": "<p>Z Avenue, 10/20/E-10/20/G, NH 66,<br>Mangalapuram, Kerala 695317</p>"
    },
    {
      "type": "text",
      "id": "contact_heading",
      "label": "Contact heading",
      "default": "Get in touch"
    },
    {
      "type": "text",
      "id": "phone",
      "label": "Phone",
      "default": "+91 90000 00000"
    },
    {
      "type": "text",
      "id": "email",
      "label": "Email",
      "default": "support@lamestonehealthcare.com"
    },
    {
      "type": "richtext",
      "id": "contact_details",
      "label": "Extra contact notes"
    },
    {
      "type": "header",
      "content": "Head office"
    },
    {
      "type": "checkbox",
      "id": "show_head_office",
      "label": "Show head office details",
      "default": true
    },
    {
      "type": "text",
      "id": "head_office_heading",
      "label": "Head office heading",
      "default": "Head Office"
    },
    {
      "type": "richtext",
      "id": "head_office_address",
      "label": "Head office address",
      "default": "<p>Bio 360, Kerala Life Sciences Industries Park,<br>Thiruvananthapuram-695317</p>"
    },
    {
      "type": "text",
      "id": "head_office_email",
      "label": "Head office email",
      "default": "mail@lamstonehealthcare.com"
    },
    {
      "type": "text",
      "id": "head_office_website",
      "label": "Head office website",
      "default": "www.lamstonehealthcare.com"
    },
    {
      "type": "richtext",
      "id": "head_office_phones",
      "label": "Head office phone numbers",
      "default": "<p><a href=\"tel:+919746397686\">+91 974 639 7686</a>, <a href=\"tel:+918139807686\">+91 813 980 7686</a><br><a href=\"tel:+918129142686\">+9181291 42686</a>, <a href=\"tel:+971547401777\">+97154 740 1777</a></p>"
    },
    {
      "type": "header",
      "content": "Footer bottom"
    },
    {
      "type": "text",
      "id": "trust_badges",
      "label": "Trust line",
      "default": "Cruelty-free · Dermatologist tested · Secure checkout"
    },
    {
      "type": "text",
      "id": "copyright_text",
      "label": "Copyright text",
      "default": "© 2026 Lamstone HealthCare Pvt Ltd. All rights reserved.\nLAME® is a skincare brand by Lamstone HealthCare Pvt Ltd.",
      "info": "Press Enter for a second line (e.g. brand disclaimer)."
    },
    {
      "type": "checkbox",
      "id": "show_policy_links",
      "label": "Show policy links",
      "default": true
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Top padding",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 48
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Bottom padding",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 32
    }
  ],
  "blocks": [
    {
      "type": "venture",
      "name": "Venture logo",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Venture name",
          "default": "Venture"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Logo image",
          "info": "Optional. Leave empty to use a theme asset filename below."
        },
        {
          "type": "text",
          "id": "asset",
          "label": "Theme asset filename",
          "info": "Example: alverstone-pharmacare-logo.png"
        },
        {
          "type": "text",
          "id": "alt",
          "label": "Alt text"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Website link"
        },
        {
          "type": "checkbox",
          "id": "open_in_new_tab",
          "label": "Open in new tab",
          "default": true
        }
      ]
    },
    {
      "type": "social_link",
      "name": "Social link",
      "settings": [
        {
          "type": "select",
          "id": "platform",
          "label": "Platform",
          "options": [
            { "value": "instagram", "label": "Instagram" },
            { "value": "facebook", "label": "Facebook" },
            { "value": "youtube", "label": "YouTube" },
            { "value": "tiktok", "label": "TikTok" },
            { "value": "pinterest", "label": "Pinterest" },
            { "value": "whatsapp", "label": "WhatsApp" },
            { "value": "linkedin", "label": "LinkedIn" }
          ],
          "default": "instagram"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Accessibility label",
          "default": "Instagram"
        },
        {
          "type": "url",
          "id": "url",
          "label": "URL"
        },
        {
          "type": "checkbox",
          "id": "open_in_new_tab",
          "label": "Open in new tab",
          "default": true
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Lame Editable Footer",
      "settings": {
        "brand_name": "Lame Beauty Care",
        "show_ventures": true,
        "ventures_heading": "Our Ventures",
        "show_newsletter": true,
        "show_map": true,
        "trust_badges": "Cruelty-free · Dermatologist tested · Secure checkout"
      },
      "blocks": {
        "venture_pharmacare": {
          "type": "venture",
          "settings": {
            "title": "Alverstone Pharmacare",
            "asset": "alverstone-pharmacare-logo.png",
            "alt": "Alverstone Pharmacare"
          }
        },
        "venture_healthcare": {
          "type": "venture",
          "settings": {
            "title": "Alverstone Healthcare Pvt Ltd",
            "asset": "alverstone-healthcare-logo.png",
            "alt": "Alverstone Healthcare Pvt Ltd"
          }
        },
        "venture_drug_house": {
          "type": "venture",
          "settings": {
            "title": "Alverstone Drug House (P) Ltd",
            "asset": "alverstone-drug-house-logo.png",
            "alt": "Alverstone Drug House (P) Ltd"
          }
        },
        "venture_lame": {
          "type": "venture",
          "settings": {
            "title": "Lamé Luxury Cosmetics & Fragrances",
            "asset": "lame-luxury-logo.png",
            "alt": "Lamé Luxury Cosmetics & Fragrances"
          }
        },
        "venture_ihed": {
          "type": "venture",
          "settings": {
            "title": "Alverstone IHED LLC",
            "asset": "alverstone-ihed-logo.png",
            "alt": "Alverstone IHED LLC"
          }
        },
        "venture_medcity": {
          "type": "venture",
          "settings": {
            "title": "Alverstone Medcity",
            "asset": "alverstone-medcity-logo.png",
            "alt": "Alverstone Medcity"
          }
        },
        "social_instagram": {
          "type": "social_link",
          "settings": {
            "platform": "instagram",
            "label": "Instagram",
            "url": "https://instagram.com"
          }
        },
        "social_facebook": {
          "type": "social_link",
          "settings": {
            "platform": "facebook",
            "label": "Facebook",
            "url": "https://facebook.com"
          }
        },
        "social_youtube": {
          "type": "social_link",
          "settings": {
            "platform": "youtube",
            "label": "YouTube",
            "url": "https://youtube.com"
          }
        }
      },
      "block_order": [
        "venture_pharmacare",
        "venture_healthcare",
        "venture_drug_house",
        "venture_lame",
        "venture_ihed",
        "venture_medcity",
        "social_instagram",
        "social_facebook",
        "social_youtube"
      ]
    }
  ]
}
{% endschema %}

