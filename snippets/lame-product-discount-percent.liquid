{%- doc -%}
  Whole-number discount percent for filters and data attributes (0 if not on sale).
  @param {object} product
{%- enddoc -%}
{%- capture lame_sale_raw -%}{%- render 'lame-product-sale-data', product: product -%}{%- endcapture -%}
{%- assign lame_sale_parts = lame_sale_raw | split: '|' -%}
{{- lame_sale_parts[0] | strip | default: 0 -}}
